/**
 * Per-route document title / meta manager (FOUNDATION stub).
 *
 * Mounted once inside <App>. Returns null for now; FE-QUALITY fills it in to
 * update document.title (and OG/meta tags) on route change.
 */
export default function RouteTitles(): null {
  return null
}
