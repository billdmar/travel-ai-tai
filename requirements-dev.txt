# Development & CI tooling.
-r requirements.txt
pytest>=8.0,<9
pytest-asyncio>=0.23,<0.25
pytest-cov>=5.0,<7
asgi-lifespan>=2.1,<3
ruff>=0.5,<1
mypy>=1.8,<2
# Optional Gemini provider SDK — dev/local only (kept out of the prod image to
# avoid grpcio/protobuf build risk; prod runs LLM_PROVIDER=mock). Required to
# run the app or tests with LLM_PROVIDER=gemini.
google-generativeai>=0.8,<0.9
